
<img alt="FormImage" src="/Content/images/Form.png" />
<form onsubmit="return false" action="Home/Form" method="POST">
    <fieldset>
        <legend>Drawing up of an order</legend>
        <input type="text" name="Name" placeholder="Enter your name" required /><br />
        <!--Тут ошибка-->
        <input type="tel" name="Tel" placeholder="8-ххх-ххх-хххх" required patern="8-[0-9]{3}-[0-9]{3}-[0-9]{4}" /><br />
        <!--Тут ошибка-->
        <select name="Car" required>
            @Html.Action("FormOptions", "Home", new {item_id= ViewBag.Item})
        </select><br />
        <input type="submit" />
    </fieldset>
</form>

